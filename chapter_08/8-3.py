def make_shirt(size, message):
    """Display the size and message printed on an ordered t-shirt."""
    print(f'Making a size {size} shirt with "{message}" written on it.')

make_shirt(8, 'Some cheesy phrase.')
make_shirt(message='Phrase cheesy some', size=15)